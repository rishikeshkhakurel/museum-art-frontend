import { styled } from "styled-components";

export const BodyHeaderStyle = styled.div`
    .title{
        color: var(--gray-500);
    }

    .search-link-container{
        display: flex;
        justify-content:space-between;
        margin: 16px 0;
    }

    button{
        cursor: pointer;
    }
`

const ArtWorkStyle = styled.div`

`

export default ArtWorkStyle